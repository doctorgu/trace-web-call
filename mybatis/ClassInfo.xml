<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="ClassInfo">
  <select id="selectHeaderInfo">
    select  name, implementsName, extendsName, annotations
    from    HeaderInfo
    where   classPath = #{classPath}
  </select>

  <select id="selectMethodInfo">
    select  annotations, isPublic, name, callers, parameterCount
    from    MethodInfo
    where   classPath = #{classPath}
  </select>
</mapper>