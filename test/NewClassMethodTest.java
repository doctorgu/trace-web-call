public class NewClassMethodTest {
  public void execute() throws Exception {
    (new BatchUtil(this)).insertJob(jobName, stepName);
    (new BatchUtil()).insertJob(jobName, stepName);
    String MMddHHmm = (new Date()).substring(4, 8);
    int val = (new Integer(0)).equals(cnt);
    String s = (String)selectForObject("bm.selectSms", paramMap);

    this.selectMember(siteId, param);
    this.memberInfoDAO.selectMember(siteId, param);
  }
}